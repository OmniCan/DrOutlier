export const metadata = {
    title: "Notes Category - Dr. Outlier",
    description: "Browse notes by category",
};

export default function CategoryLayout({ children }) {
    return <>{children}</>;
}
