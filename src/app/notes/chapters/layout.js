export const metadata = {
    title: "Notes Chapters - Dr. Outlier",
    description: "Browse notes by chapter",
};

export default function ChaptersLayout({ children }) {
    return <>{children}</>;
}
