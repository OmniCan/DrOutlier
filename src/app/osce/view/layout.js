export const metadata = {
    title: "OSCE Case - Dr Outlier Radiology",
    description: "Practice OSCE case with questions, answers, and detailed explanations.",
};

export default function OSCEViewLayout({ children }) {
    return <>{children}</>;
}
