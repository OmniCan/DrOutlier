export const metadata = {
    title: "Spotters Category - Dr. Outlier",
    description: "Browse spotters by category",
};

export default function CategoryLayout({ children }) {
    return <>{children}</>;
}
